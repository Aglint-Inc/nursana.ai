'use client';

import { Body } from '@/campaigns/components/Body';
import { Table } from '@/campaigns/components/Table';

const Page = () => {
  return (
    <Body>
      <Table />
    </Body>
  );
};

export default Page;
